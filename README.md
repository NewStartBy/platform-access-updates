# platform-access updates

Public signed update metadata for platform-access.

Release binaries remain in the private NewStartBy/platform repository.
The release pipeline publishes latest.json and latest.json.sig together.

No update channel has been published yet.
